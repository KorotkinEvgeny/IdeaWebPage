export class Card {
    constructor(
        id: number,
        header: string,
        text: string,
        category: string
    ) { }
    // id: number;
    // header: string;
    // text: string;
    // category:string;
}